{
  "name": "orchestrator",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test": "rimraf dist/test  && cross-env NODE_OPTIONS=--experimental-vm-modules jest --runInBand --detectOpenHandles",
    "build": "rimraf dist && npx tsc"
  },
  "type": "module",
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "@enterprise/kafka-common": "*",
    "kafkajs": "^2.2.4",
    "rimraf": "^6.1.3",
    "uuid": "^14.0.1"
  },
  "devDependencies": {
    "@types/node": "^25.9.5",
    "cross-env": "^10.1.0"
  }
}
